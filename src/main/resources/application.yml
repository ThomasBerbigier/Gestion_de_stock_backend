spring:
    datasource:
        url: jdbc:mariadb://localhost:3306/gestionstock
        username: administrateur_gs
        password: +Mb9UT!T]zbWU'+
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        database: MYSQL
        show-sql: false
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQLDialect

server:
    port: 8081